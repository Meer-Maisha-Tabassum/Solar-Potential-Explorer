generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model FinancialModel {
  id        Int      @id @default(autoincrement())
  modelType String   @unique // "PPA" or "UPFRONT"
  data      Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}